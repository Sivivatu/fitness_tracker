from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A synchronization and tracking process to analysis fitness data from multiple sources',
    author='Paul Houghton',
    license='',
)
